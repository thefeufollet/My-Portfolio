# Core (training + local inference)
transformers>=4.41,<5
setfit>=1.1.3
datasets>=4.4.1
pandas>=2.3.3
torch>=2.6.0
sentence-transformers>=3.4.0
scikit-learn>=1.6.0
modal>=1.0.0
python-dotenv>=1.0.0

# Optional: only for local W&B Weave evaluation (uv run eval.py)
weave>=0.52.22
wandb>=0.19.0
